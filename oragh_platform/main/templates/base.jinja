<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
      /* Sticky navbar */
      .navbar {
        position: sticky !important;
        top: 0;
        z-index: 1020;
        box-shadow: 0 2px 4px rgba(0,0,0,.1);
      }
      
      /* Body padding for sticky navbar */
      body {
        padding-top: 0;
      }
      
      /* Enhanced navbar brand */
      .navbar-brand {
        font-size: 1rem;
        font-weight: 500;
        display: flex !important;
        align-items: center !important;
        line-height: 1.2;
        padding: 0.5rem 0;
      }
      
      .navbar-brand span {
        white-space: nowrap;
        transition: all 0.3s ease;
      }
      
      .navbar-brand img {
        transition: all 0.3s ease;
      }
      
      /* Better navbar alignment */
      .navbar-collapse {
        align-items: center;
      }
      
      .navbar-nav {
        align-items: center;
        gap: 0.5rem;
      }
      
      .navbar-nav .nav-link {
        padding: 0.5rem 1rem;
        border-radius: 0.375rem;
        transition: all 0.3s ease;
        font-weight: 500;
      }
      
      .navbar-nav .nav-link:hover {
        background-color: rgba(255, 255, 255, 0.1);
        color: #fff !important;
      }
      
      /* Enhanced dropdown styling */
      .dropdown-menu {
        background-color: #fff;
        border: 1px solid rgba(0, 0, 0, 0.15);
        border-radius: 0.5rem;
        box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
        margin-top: 0.25rem;
        padding: 0.5rem 0;
        min-width: 180px;
      }
      
      .dropdown-item {
        padding: 0.5rem 1.25rem;
        font-weight: 500;
        color: #495057;
        transition: all 0.2s ease;
        border: none;
        background: none;
        width: 100%;
        text-align: left;
      }
      
      .dropdown-item:hover,
      .dropdown-item:focus {
        background-color: #f8f9fa;
        color: #212529;
      }
      
      .dropdown-item:active {
        background-color: #e9ecef;
        color: #212529;
      }
      
      .dropdown-toggle::after {
        margin-left: 0.5rem;
        transition: transform 0.3s ease;
      }
      
      .dropdown-toggle[aria-expanded="true"]::after {
        transform: rotate(180deg);
      }
      
      /* User profile image in dropdown */
      .dropdown-toggle img {
        border: 2px solid rgba(255, 255, 255, 0.3);
        transition: border-color 0.3s ease;
      }
      
      .dropdown-toggle:hover img {
        border-color: rgba(255, 255, 255, 0.6);
      }
      
      /* Mobile responsive adjustments */
      @media (max-width: 991px) {
        .navbar-nav {
          text-align: center;
          margin-top: 1rem;
        }
        
        .navbar-nav .nav-link {
          margin: 0.25rem 0;
          padding: 0.75rem 1rem;
        }
        
        .dropdown-menu {
          position: static !important;
          transform: none !important;
          box-shadow: none;
          border: none;
          background-color: rgba(255, 255, 255, 0.1);
          border-radius: 0.5rem;
          margin-top: 0.5rem;
        }
        
        .dropdown-item {
          color: rgba(255, 255, 255, 0.9);
          text-align: center;
        }
        
        .dropdown-item:hover {
          background-color: rgba(255, 255, 255, 0.1);
          color: #fff;
        }
      }
      
      @media (max-width: 768px) {
        .navbar-brand {
          font-size: 0.9rem;
        }
        .navbar-brand img {
          height: 32px !important;
          margin-right: 8px !important;
        }
      }
      
      @media (max-width: 576px) {
        .navbar-brand {
          font-size: 0.8rem;
        }
        .navbar-brand img {
          height: 28px !important;
          margin-right: 6px !important;
        }
        
        .navbar {
          padding: 0.5rem 0;
        }
        
        .container {
          padding-left: 1rem;
          padding-right: 1rem;
        }
      }
      
      @media (max-width: 400px) {
        .navbar-brand {
          font-size: 0.75rem;
        }
        .navbar-brand span {
          font-size: 0.75rem;
        }
        .navbar-brand img {
          height: 24px !important;
          margin-right: 4px !important;
        }
      }
      
      /* Container padding adjustment for sticky navbar */
      .container.my-4 {
        margin-top: 1.5rem !important;
        margin-bottom: 1.5rem !important;
      }
    </style>
    {% block extra_css %}{% endblock %}
    <title>{% block title %}{% endblock %}</title>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/">
          <img src="/media/general/Logo_ORAGH_footer-e1739129105993.png" alt="ORAGH Logo" style="height: 40px; margin-right: 12px;">
          <span>Orkiestra Reprezentacyjna AGH</span>
        </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbar">
          {% if user.is_authenticated %}
            <ul class="navbar-nav mx-auto my-2 my-lg-0 gap-0 gap-lg-3">
              <li class="nav-item">
                <a class="nav-link" href="/concerts">Koncerty</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/attendance">Frekwencja</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/forum">Forum</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/band/">Aktualny Skład</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/profiles/">Użytkownicy</a>
              </li>
            </ul>
            
            <ul class="navbar-nav mb-2 mb-lg-0">
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  {% if user.musicianprofile.photo %}
                    <img src="{{ user.musicianprofile.photo.url }}" alt="User photo" class="rounded-circle me-2" style="width: 32px; height: 32px; object-fit: cover;">
                  {% endif %}
                  {{ user.username }}
                </a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                  <li><a class="dropdown-item" href="/profile/">⚙️ Ustawienia</a></li>
                  <li>
                    <form method="post" action="{% url 'logout' %}" style="margin: 0;">
                      {% csrf_token %}
                      <button type="submit" class="dropdown-item">🚪 Wyloguj</button>
                    </form>
                  </li>
                </ul>
              </li>
            </ul>
          {% else %}
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link" href="/register">Rejestracja</a>
              </li>
            </ul>
          {% endif %}
        </div>
      </div>
    </nav>
    <div class="container my-4">
    {% if messages %}
      {% for message in messages %}
        {% if message.tags == "error" %}
          <div class="alert alert-danger alert-dismissible fade show" role="alert">
        {% else %}
          <div class="alert alert-{{ message.tags }} alert-dismissible fade show" role="alert">
        {% endif %}
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
      {% endfor %}
    {% endif %}
    {% block page_header %}
      <div class="row justify-content-between align-items-center mb-4">
          <h1 class="col-12 mb-0">
            {% block page_title %}{% endblock %}
          </h1>
      </div>
    {% endblock %}
    {% block content %}{% endblock %}
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% block extra_js %}{% endblock %}
  </body>
</html>
