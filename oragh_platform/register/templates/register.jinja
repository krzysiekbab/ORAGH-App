{% extends "base.jinja" %}

{% block title %}Rejestracja{% endblock %}

{% block page_header %}
    <div class="row justify-content-between align-items-center mb-4">
        <h1 class="col-12 mb-0 text-center">
            Rejestracja nowego użytkownika
        </h1>
    </div>
{% endblock %}

{% block content %}
<form method="post" class="mb-4">
  {% csrf_token %}
  
  <div class="card">
    <div class="card-header">
      <h5 class="mb-0">Dołącz do ORAGH</h5>
    </div>
    <div class="card-body">
      <p class="text-muted mb-4">
        Wypełnij poniższy formularz, aby dołączyć do orkiestry. 
        Wszystkie pola są wymagane.
      </p>
      
      <div class="row g-3">
        <div class="col-md-6">
          <label for="id_first_name" class="form-label">Imię</label>
          {{ form.first_name }}
          {% if form.first_name.errors %}
            <div class="text-danger small">{{ form.first_name.errors }}</div>
          {% endif %}
        </div>
        
        <div class="col-md-6">
          <label for="id_last_name" class="form-label">Nazwisko</label>
          {{ form.last_name }}
          {% if form.last_name.errors %}
            <div class="text-danger small">{{ form.last_name.errors }}</div>
          {% endif %}
        </div>
        
        <div class="col-md-6">
          <label for="id_username" class="form-label">Nazwa użytkownika</label>
          {{ form.username }}
          <div class="form-text">Unikalny identyfikator w systemie</div>
          {% if form.username.errors %}
            <div class="text-danger small">{{ form.username.errors }}</div>
          {% endif %}
        </div>
        
        <div class="col-md-6">
          <label for="id_email" class="form-label">Adres email</label>
          {{ form.email }}
          <div class="form-text">Używany do komunikacji</div>
          {% if form.email.errors %}
            <div class="text-danger small">{{ form.email.errors }}</div>
          {% endif %}
        </div>
        
        <div class="col-md-6">
          <label for="id_instrument" class="form-label">Instrument</label>
          {{ form.instrument }}
          <div class="form-text">Twój główny instrument</div>
          {% if form.instrument.errors %}
            <div class="text-danger small">{{ form.instrument.errors }}</div>
          {% endif %}
        </div>
        
        <div class="col-md-6">
          <label for="id_birthday" class="form-label">Data urodzenia</label>
          {{ form.birthday }}
          {% if form.birthday.errors %}
            <div class="text-danger small">{{ form.birthday.errors }}</div>
          {% endif %}
        </div>
        
        <div class="col-md-6">
          <label for="id_password1" class="form-label">Hasło</label>
          {{ form.password1 }}
          <div class="form-text">Minimum 8 znaków</div>
          {% if form.password1.errors %}
            <div class="text-danger small">{{ form.password1.errors }}</div>
          {% endif %}
        </div>
        
        <div class="col-md-6">
          <label for="id_password2" class="form-label">Potwierdź hasło</label>
          {{ form.password2 }}
          <div class="form-text">Wprowadź hasło ponownie</div>
          {% if form.password2.errors %}
            <div class="text-danger small">{{ form.password2.errors }}</div>
          {% endif %}
        </div>
      </div>
      
      {% if form.non_field_errors %}
        <div class="alert alert-danger mt-3">
          {{ form.non_field_errors }}
        </div>
      {% endif %}
    </div>
  </div>

  <div class="d-flex justify-content-between mt-4">
    <a href="{% url 'login' %}" class="btn btn-outline-secondary">Mam już konto</a>
    <button type="submit" class="btn btn-primary">Zarejestruj się</button>
  </div>
</form>

<style>
/* Enhanced mobile responsiveness */
@media (max-width: 768px) {
  .d-flex.justify-content-between {
    flex-direction: column;
    gap: 1rem;
  }
  
  .d-flex.justify-content-between .btn {
    width: 100%;
    padding: 0.75rem;
  }
  
  .card-header h5 {
    font-size: 1.125rem;
  }
  
  .row.g-3 {
    margin: 0;
  }
  
  .row.g-3 > [class*="col-"] {
    padding: 0.75rem 0;
  }
  
  .form-control, .form-select {
    font-size: 16px; /* Prevents zoom on iOS */
  }
  
  .form-text {
    font-size: 0.8rem;
    margin-top: 0.5rem;
  }
}

@media (max-width: 576px) {
  .card {
    border-radius: 0.25rem;
    margin: 0 -0.5rem;
  }
  
  .card-body {
    padding: 1rem;
  }
  
  .card-header {
    padding: 0.75rem 1rem;
  }
  
  .form-label {
    font-weight: 600;
    margin-bottom: 0.5rem;
  }
  
  .btn {
    font-size: 0.9rem;
    padding: 0.75rem 1rem;
  }
  
  .col-md-6 {
    flex: 0 0 100%;
    max-width: 100%;
  }
}

@media (max-width: 375px) {
  /* Extra small screens */
  .container-fluid, .container {
    padding-left: 0.5rem;
    padding-right: 0.5rem;
  }
  
  .card {
    margin: 0 -0.25rem;
  }
  
  .card-body {
    padding: 0.75rem;
  }
  
  .btn {
    font-size: 0.85rem;
    padding: 0.6rem 0.8rem;
  }
}

/* Improve form accessibility */
.form-control:focus, .form-select:focus {
  border-color: #86b7fe;
  box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25);
}

.form-text {
  color: #6c757d;
  line-height: 1.4;
}

.form-control, .form-select {
  border: 1px solid #ced4da;
  border-radius: 0.375rem;
  padding: 0.375rem 0.75rem;
  width: 100%;
}

.form-label {
  margin-bottom: 0.5rem;
  font-weight: 500;
}
</style>
{% endblock %}  