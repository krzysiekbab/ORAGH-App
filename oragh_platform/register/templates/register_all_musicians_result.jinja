{% extends "base.jinja" %}

{% block title %}Rejestracja muzyków{% endblock %}
{% block page_title %}Rejestracja zakończona{% endblock %}

{% block content %}
<div class="row justify-content-center">
  <div class="col-md-8 col-lg-6">
    <div class="card">
      <div class="card-header bg-success text-white">
        <h5 class="mb-0">✅ Rejestracja muzyków</h5>
      </div>
      <div class="card-body text-center">
        <div class="mb-4">
          <i class="fas fa-users fa-3x text-success mb-3"></i>
          <h6>Operacja zakończona pomyślnie</h6>
        </div>
        
        <div class="row g-3">
          <div class="col-6">
            <div class="card border-success">
              <div class="card-body">
                <h3 class="text-success mb-1">{{ created_count }}</h3>
                <p class="small mb-0">Nowi użytkownicy</p>
              </div>
            </div>
          </div>
          
          <div class="col-6">
            <div class="card border-warning">
              <div class="card-body">
                <h3 class="text-warning mb-1">{{ skipped_count }}</h3>
                <p class="small mb-0">Pominięto (istnieją)</p>
              </div>
            </div>
          </div>
        </div>
        
        <div class="alert alert-info mt-4">
          <strong>Podsumowanie:</strong>
          <br>
          Dodano <strong>{{ created_count }}</strong> nowych użytkowników
          <br>
          Pominięto <strong>{{ skipped_count }}</strong> (już istnieją w systemie)
        </div>
        
        <div class="mt-4">
          <a href="{% url 'home' %}" class="btn btn-primary">
            🏠 Powrót do strony głównej
          </a>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
@media (max-width: 768px) {
  .col-md-8.col-lg-6 {
    flex: 0 0 100%;
    max-width: 100%;
  }
  
  .card {
    margin: 0 -0.5rem;
  }
  
  .fa-3x {
    font-size: 2rem !important;
  }
  
  .btn {
    font-size: 0.9rem;
    padding: 0.75rem 1rem;
  }
  
  .row.g-3 .col-6 {
    margin-bottom: 0.5rem;
  }
}

@media (max-width: 576px) {
  .card-body {
    padding: 1rem;
  }
  
  .card-header {
    padding: 0.75rem 1rem;
  }
  
  .alert {
    padding: 0.75rem;
    font-size: 0.9rem;
  }
  
  h6 {
    font-size: 1rem;
  }
  
  h3 {
    font-size: 1.5rem;
  }
  
  .small {
    font-size: 0.8rem;
  }
}

.card-header.bg-success {
  border-bottom: 1px solid rgba(255,255,255,0.2);
}

.fa-users {
  color: #198754;
}

.card.border-success .card-body,
.card.border-warning .card-body {
  padding: 0.75rem;
}
</style>
{% endblock %}
